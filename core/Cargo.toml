[package]
name = "otm-core"
description = "System data collection shared by the Open Task Manager GUI and TUI"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sysinfo = "0.32"
